var inv = function() {
    var a = {
            slots: ["sword", "empty"]
        },
        d = function(b) {
            for (var a in d) {
                if (d == b) {
                    return d[a];
                }
            }
        },
        e = function(b) {
            return Object.keys(a).includes(b) ? parseInt(a[b], 10) : "0000";
        };
    return {
        dat: a,
        key: d,
        val: e,
        print: function() {
            console.log(a);
        },
        add: function(b, c) {
            if (Object.keys(a).includes(b)) {
                var d = e(b) + (c ? c : 0);
                a[b] = d;
            } else {
                a[b] = c ? c : 1;
            }
        },
        put: function(b, c) {
            a[b] = c ? c : 1;
        },
        remove: function(b) {
            delete a[b];
        },
        one: a.slots[0],
        two: a.slots[1]
    };
}();